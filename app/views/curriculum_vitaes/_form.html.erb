<%= form_for(@curriculum_vitae, html: { multipart: true, class: 'form-horizontal' }) do |f| %>
  <% if @curriculum_vitae.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@curriculum_vitae.errors.count, 'error') %> prohibited this curriculum_vitae from being saved:</h2>

      <ul>
      <% @curriculum_vitae.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, class: 'col-sm-2 control-label' %>
    <div class="col-sm-10">
      <%= f.text_field :name, class: 'form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :description, class: 'col-sm-2 control-label' %>
    <div class="col-sm-10">
      <%= f.text_area :description, class: 'form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :upload, class: 'col-sm-2 control-label' %>
    <div class="col-sm-10">
      <%= f.file_field :upload, class: 'form-control' %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-10 col-sm-offset-2">
      <%= f.submit 'Save', class: 'btn btn-success' %>
      or
      <%= link_to 'Cancel', curriculum_vitaes_path %>
    </div>
  </div>
<% end %>
