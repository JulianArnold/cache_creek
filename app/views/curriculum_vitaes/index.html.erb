<%= page_heading('Curriculum Vitaes') %>

<div class="row">
  <div class="col-sm-12">
    <table class="table">
      <thead>
        <tr>
          <th>Name</th>
          <th>Description</th>
          <th>Upload</th>
          <th colspan="2"></th>
        </tr>
      </thead>

      <tbody>
        <% @curriculum_vitaes.each do |curriculum_vitae| %>
          <tr>
            <td><%= link_to curriculum_vitae.name, curriculum_vitae %></td>
            <td><%= curriculum_vitae.description %></td>
            <td><%= curriculum_vitae.upload_file_name %></td>
            <td>
              <%= link_to edit_curriculum_vitae_path(curriculum_vitae) do %>
                <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
              <% end %>
            </td>
            <td>
              <%= link_to curriculum_vitae, method: :delete, data: { confirm: 'Are you sure?' } do %>
                <span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <p><%= link_to 'New Curriculum Vitae', new_curriculum_vitae_path, class: 'btn btn-success' %></p>
  </div>
</div>
